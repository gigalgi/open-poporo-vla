# open-poporo-vla
vision-language-action models that run on hardware people actually own. Edge AI, real robot data, no cloud required. Same mission as the hardware: capable robots shouldn't need a data center.
